package SpiegazioniAdvanced;

public class Astratta {
   /*
   * I costruttori sono ammessi.
Possiamo avere una classe astratta senza alcun metodo astratto.
   *
   *non ci possono essere metodi final
   *
   *
   *
   *
   *  In Java, possiamo avere una classe astratta senza alcun metodo astratto . Questo ci
   * permette di creare classi che non possono essere istanziate ma possono solo essere ereditate .
   *
   * Una classe astratta è una classe che non può essere avviata da sola, deve essere sottoclassata da un'altra classe per utilizzare le sue proprietà.
Una classe astratta può essere creata utilizzando parole chiave "abstract".
Possiamo avere una classe astratta senza alcun metodo astratto.
   *
   *
   *
   *
   * INTERFACCE
   *
   *  specificare il comportamento di una classe.
   * Un'interfaccia Java contiene costanti statiche e metodi astratti.
   * Possono esserci solo metodi astratti nell'interfaccia Java, non nel corpo del metodo.
   *
   * Quando decidiamo un tipo di entità in base al
   * suo comportamento e non tramite attributo, dovremmo definirla come un'interfaccia.
   *
   *Le interfacce specificano cosa deve fare una classe e non come. È il modello del comportamento.
   * L'interfaccia non ha un costruttore.

* Se una classe implementa un'interfaccia e non fornisce i corpi del metodo per tutte le funzioni
*  specificate nell'interfaccia, allora la classe deve essere dichiarata astratta.
*
   *USIAMO ui
   * Le interfacce vengono utilizzate per implementare l'astrazione
   * Viene utilizzato per ottenere l'astrazione totale.

   *
   *
   *
   *
   * In Java non sono consentite eredità multiple, tuttavia è possibile utilizzare
   * un'interfaccia per utilizzarla poiché è possibile implementare più di un'interfaccia.
   *
   *
   *
   *
   *
   * MEWS
   * All'interno dell'interfaccia non sono possibili dichiarazioni di metodi statici, finali e privati.
Nuove funzionalità aggiunte nelle interfacce in JDK 9
Da Java 9 in poi, le interfacce possono contenere anche quanto segue:

Metodi statici
Metodi privati
Metodi statici privati
   * */



   /*




    Un'interfaccia di confronto viene utilizzata per ordinare gli oggetti delle classi definite dall'utente
confronta due obj della stessa classe
public int compare(Object obj1, Object obj2):


*/
}
